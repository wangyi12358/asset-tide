# AssetAtlas｜多资产人民币估值平台产品需求文档

- 项目名称：AssetAtlas
- 仓库名称：`asset-atlas`
- 英文简介：A personal wealth dashboard for tracking multi-asset portfolios in CNY.
- 版本：v1.0（首版开发基线）
- 日期：2026-09-18
- 产品形态：响应式 Web 应用
- 技术方向：React + TypeScript 前端，Node.js + TypeScript 后端
- 基准币种：人民币 CNY
- 文档状态：可用于页面设计、任务拆分与开发验收

## 1. 产品定位

AssetAtlas 意为“资产地图”，为个人提供一个统一查看现金、股票、基金、黄金和加密货币价值的资产看板。用户注册登录后，手动录入持仓与后续变动，系统获取市场价格和汇率，计算人民币参考价值，并持续记录资产变化。

首版以个人自用为主要场景，但支持多个用户独立注册与使用。每个用户的账户、持仓、流水和统计数据完全隔离。

### 1.1 核心价值

1. 不必逐个平台查看资产，统一按人民币汇总。
2. 同时看见资产数量、原币价值、人民币价值与行情更新时间。
3. 区分资产增加和投资收益，避免把新增存款误认为盈利。
4. 手机、平板和电脑均可完成录入、查询和修改。

### 1.2 已确定需求与设计默认值

| 项目 | 内容 | 属性 |
| --- | --- | --- |
| 项目名称 | AssetAtlas | 已确定 |
| 仓库名称 | `asset-atlas` | 已确定 |
| 客户端 | 响应式 Web | 已确定 |
| 用户功能 | 注册、登录 | 已确定 |
| 后端 | Node.js | 已确定 |
| 资产范围 | 外币现金、股票、黄金、加密货币，并延续国内基金支持 | 需求归纳 |
| 展示币种 | 人民币 | 已确定 |
| 核心图表 | 历史资产波动图 | 已确定 |
| 前端方案 | React + TypeScript | 设计默认值 |
| 身份方式 | 邮箱 + 密码，无需邮箱验证，支持密码找回 | 设计默认值 |
| 更新策略 | 页面按需刷新 + 后台定时采集，每日保存快照 | 设计默认值 |
| 界面语言 | 简体中文 | 设计默认值 |

## 2. 首版范围

### 2.1 必须交付（P0）

- 注册、自动登录、退出、找回密码。
- 用户数据隔离与基本账户安全。
- 总资产首页、资产列表、资产详情、资产录入与流水页面。
- 人民币、美元、港元现金；A 股、港股、美股；国内公募基金；黄金；主流加密货币。
- 默认资产账户及自定义账户，用于区分银行、券商、钱包等持仓位置。
- 手动录入期初持仓，记录买卖、出入金、内部转账、换汇、分红和持仓调整。
- 自动获取参考价格和汇率；接口异常时支持有标识的手动报价。
- 人民币总资产估值、资产类型占比、累计投资盈亏金额。
- 从开始记录起生成总资产曲线和累计投资盈亏曲线。
- 数据导出、更新时间提示、异常与空状态处理。
- 手机、平板、桌面端响应式适配。

### 2.2 后续迭代（P1）

- CSV/券商账单导入及历史记录补录。
- 时间加权收益率、资金加权收益率、年化收益率和最大回撤。
- 汇率贡献与资产价格贡献拆分。
- 基准指数对比、目标资产配置与偏离提醒。
- PWA 安装、深色模式、价格提醒、双因素认证。
- 房产等低频估值资产，以及贷款等负债管理。

### 2.3 首版不包含

- 自动登录银行或券商、读取真实账户余额、链上钱包自动同步。
- 下单、支付、充值、托管或任何资产交易执行。
- 杠杆、融资融券、做空、期权和其他衍生品。
- 实时逐笔行情、公开排行、社交及投资建议。
- 原生 iOS/Android 应用和应用商店发布。

## 3. 用户流程

### 3.1 首次使用

注册 → 自动登录 → 阅读简短引导 → 创建或使用默认账户 → 录入期初资产 → 获取行情 → 查看首次估值。

引导说明：系统不会自动读取金融账户；估值依赖用户维护的数量；真实历史曲线从记录开始积累。

### 3.2 日常使用

登录 → 查看总资产与行情状态 → 按需刷新 → 查看资产详情或历史曲线 → 录入新增交易 → 估值重新计算。

### 3.3 行情异常

查看异常提示 → 使用最近有效报价并显示日期，或填写手动参考价 → 恢复自动报价后按规则切换。

手动报价不能静默覆盖自动报价，来源和生效时间必须可见。用户可主动结束手动报价；行情恢复时提示用户切回自动模式。

## 4. 页面与功能

### 4.1 注册、登录与密码找回

| 页面 | 功能与规则 |
| --- | --- |
| 注册 | 邮箱、密码、确认密码；校验格式和一致性；注册成功后自动建立登录会话，无需发信 |
| 登录 | 邮箱与密码；账号或密码错误使用统一提示；已有未验证账户同样可以登录 |
| 找回密码 | 无论邮箱是否存在，都展示统一结果；重置链接一次性有效，默认 30 分钟过期 |
| 重置密码 | 设置新密码后撤销原有登录会话 |
| 退出 | 撤销当前会话并返回登录页 |

- 密码长度为 8–128 个字符，支持密码管理器、粘贴和自动填充。
- 输入密码提供显示/隐藏按钮，错误提示紧邻对应输入项。
- 页面支持安全的站内返回地址，登录成功后回到原访问页面。
- 会话默认闲置 7 天过期、最长 30 天；修改密码等敏感操作要求重新验证。
- 注册和登录不依赖邮件服务；密码重置邮件为找回密码功能的上线依赖。

### 4.2 资产总览 `/dashboard`

按优先级展示：

1. 人民币总资产、估值时间、数据是否完整。
2. 较上一有效日快照的资产变化、期间净流入、投资盈亏金额。
3. 总资产 / 累计投资盈亏切换图，支持近 7 天、近 30 天、近 90 天、全部。
4. 资产类型占比图与可读数值列表。
5. 主要持仓列表、最近流水和行情异常提醒。

操作入口：添加资产、记录变动、刷新估值。

没有资产时显示录入引导。只有一个历史点时显示单点和“历史数据积累中”，不虚构曲线。暂时不能计算的指标显示“—”及原因。

### 4.3 资产列表 `/assets`

- 字段：名称、代码、市场、所属账户、数量、原币单价、人民币价值、占比、报价时间与状态。
- 支持名称/代码搜索，以及资产类型、账户筛选。
- 支持按人民币价值、名称排序。
- 默认按账户和标的展示持仓；首页可汇总同一标的。
- 已清仓资产默认隐藏，可切换查看。
- 首版不展示缺少成本依据的持仓收益率。

### 4.4 添加资产与期初持仓

| 资产类型 | 必填信息 | 补充规则 |
| --- | --- | --- |
| 现金 | 币种、余额、账户 | 币种使用标准代码，不以名称自由匹配 |
| 股票 | 市场、证券代码、股数、账户 | 支持碎股；使用市场和代码共同识别标的 |
| 基金 | 基金代码、份额、账户 | A/C 等份额类别分别识别 |
| 黄金 | 品种、克数、纯度、报价基准、账户 | 默认足金金条；纯度与基准必须明确 |
| 加密货币 | 币种唯一 ID、数量、账户 | 按供应商 ID 识别，同名币不可仅按简称匹配 |

- 购买成本和备注可选；缺少成本不影响当前估值。
- 搜索标的时展示名称、代码、市场、报价币种，避免误选。
- 无自动行情覆盖时，可建立手动报价标的，明确显示“手动估值”。
- 首次引导中的持仓共同组成期初资产；初始化完成后，新增资产必须说明是外部转入、交易买入，还是遗漏持仓修正。
- 期初资产通过统一估值后才建立正式起始基线；缺价时先显示部分估值，不开始计算完整组合盈亏。

### 4.5 资产详情 `/assets/:id`

- 展示持有数量、原币价值、人民币参考价值、使用的汇率及数据来源。
- 展示当前行情状态和报价时间。
- 分别展示标的价格走势与用户持仓价值走势，名称与单位清晰区分。
- 标的价格可早于用户加入日期；用户持仓价值不能用今天的数量倒推过去。
- 展示相关流水，支持记录变动、修正记录和调整报价方式。
- 支持直接设置当前持仓总数量（非负十进制），按增减差额记录持仓修正；期初录入阶段同样可用。
- 支持删除当前账户下的持仓：确认后将数量设为 0，从默认列表移除，保留历史流水；可通过“显示已清仓资产”查看并恢复数量。
- 数量修改与删除校验用户、账户及操作前数量，拒绝覆盖并发修改；初始化后按参考估值调整收益归因，缺少报价/汇率时要求补充依据，不将删除误算为投资亏损。
- 没有历史价格时保留当前估值与流水，不阻塞整个页面。

### 4.6 流水 `/transactions`

| 类型 | 处理方式 | 是否计入外部净流入 |
| --- | --- | --- |
| 期初持仓 | 建立开始记录时的基线 | 不计入后续净流入 |
| 外部转入/转出 | 现金或资产进入/离开本组合 | 是，以发生时人民币价值计算 |
| 买入/卖出 | 证券或币数量与结算现金同时变动，记录费用 | 否 |
| 内部转账 | 本组合两个账户成对增减，支持记录费用 | 否 |
| 换汇 | 记录实际支付与收到的两种货币数量及费用 | 否 |
| 分红/利息 | 现金余额增加；再投资则同时记录买入 | 否，属于组合收益 |
| 拆股/份额变更 | 按事件调整数量，保留原因 | 否，不凭空产生收益 |
| 持仓修正 | 纠正遗漏或错误，并记录原因及影响范围 | 不直接视为收益，需重算或建立调整基线 |

- 每条流水包含发生时间、账户、类型、资产、数量、币种、关联现金变动、费用和备注。
- 买卖和内部转账以一个完整事件保存，避免只更新一侧造成资产重复计算。
- 买入前检查结算账户余额，首版不允许负余额或负持仓；余额不足时引导先记录外部入金。
- 卖出数量不能超过持仓，数量和金额使用十进制精度计算。
- 首版支持开始记录时间之后的补记；早于基线的完整历史导入放入后续迭代。
- 编辑或撤销已影响历史的流水时，展示影响日期，保留审计记录，并重算受影响快照。
- 重算中显示状态，不把新旧数据混合为已完成结果；缺少必要历史行情时明确标记待补全。

### 4.7 账户与设置 `/settings`

- 管理逻辑账户名称，如“银行美元账户”“美股券商”“冷钱包”。
- 修改密码、查看登录邮箱、退出登录。
- 人民币为首版固定统计币种，默认显示时区为 Asia/Shanghai。
- 导出持仓、流水、快照为 CSV，导出完整个人数据为带版本号的 JSON。
- 非空账户不能直接删除；可归档并保留历史数据。

## 5. 估值与收益规则

### 5.1 基本公式

```text
单项人民币价值 = 持有数量 × 原币单价 × 原币兑人民币汇率
现金人民币价值 = 原币余额 × 原币兑人民币汇率
组合总资产 = 各项人民币价值之和
```

汇率统一定义为“1 单位原币值多少人民币”；人民币汇率固定为 1，避免倒数混用。

黄金报价若为人民币/克，直接按含金重量计算；若为美元/金衡盎司，需要先统一重量单位，再换算人民币。黄金 ETF 按证券份额估值，不能作为实物克数处理。实物黄金页面标注为参考金属价值，首版不自动估算回收折价或首饰工费。

稳定币按自身行情价格估值，不固定等同美元。股票当前估值使用实际市场价格；历史持仓估值使用当时未复权价格和当时实际股数，分红、拆股另行处理。

### 5.2 资产变化与投资盈亏

```text
期间资产变化 = 期末总资产 − 期初总资产
期间外部净流入 = 外部转入人民币价值 − 外部转出人民币价值
期间投资盈亏 = 期间资产变化 − 期间外部净流入
累计投资盈亏 = 当前总资产 − 起始基线资产 − 起始以来外部净流入
```

- 外部现金流按发生时的汇率折算；外部转入证券等非现金资产，按发生时参考价值确认流入。
- 缺少发生时价格或汇率时，要求用户补充有依据的参考值并记录来源；在补全前相关盈亏显示待确认。
- 手续费计入组合损益；内部转账本身不产生外部现金流。
- 首版展示盈亏金额，不将简单资产增幅包装为投资收益率。
- 总览默认使用“较上一日快照”，不使用容易误解的“今日交易收益”。

### 5.3 历史快照

- 每天北京时间 08:00 为统一估值时点；记录准确的 `asOf` 时间，并在图表中可查看。
- 每项资产选取该时点之前最近有效的价格与汇率，各自保留原始行情时间。
- 当前估值可按需更新；历史日快照与最新临时估值分开存储和展示。
- 休市沿用最近有效报价并标注；缺失首次价格不填零，不使用未来报价回填过去。
- 迟到数据按其有效时间修订历史快照，记录版本，不能把更晚交易时点的价格当作较早价格。
- 定时任务支持幂等、失败重试和断点补跑；历史数据不足时保留缺口并解释原因。
- 历史持仓数量来自流水重建；补记事件引起的快照重算应可追溯。

### 5.4 估值完整性与精度

- 全部持仓都有可用价格和汇率时，显示总资产；含过期报价时标注“含历史报价”。
- 任一持仓缺少价格或汇率时，显示“已估值资产合计”和未估值项数，不能称作完整总资产。
- 不完整区间的组合盈亏不作确定展示；图表断点或使用明确的待补全标识。
- 手动价格、自动价格、休市报价、异常陈旧报价具有不同状态。
- 数据库与计算过程使用 Decimal 或等效十进制方案，避免二进制浮点误差累积。
- 人民币金额默认展示两位小数；数量与行情保留资产所需精度，不在中间步骤提前舍入。

## 6. 行情与汇率接入

以下为候选接入方向，开发前需用实际持仓标的验证覆盖、授权、额度、时效与部署网络可用性。不承诺全部数据免费或实时。

| 数据 | 候选来源 | 首版要求 |
| --- | --- | --- |
| 法币汇率 | Frankfurter 等汇率 API | 支持 CNY、USD、HKD 及历史日期 |
| 美股 | Twelve Data 等行情服务 | 最新可用价、报价币种、报价时间、日线 |
| A 股、港股 | 可授权使用的 HTTP 行情服务 | 明确市场、证券代码与交易时段 |
| 国内基金 | Tushare 等净值数据服务 | 单位净值与净值日期，区别估算值和已公布净值 |
| 黄金 | 上海黄金交易所公开行情或获授权的金价 API | 明确品种、纯度、币种和重量单位 |
| 加密货币 | CoinGecko 等行情 API | 唯一币种 ID、价格、时间和历史数据 |

Node.js 后端统一提供数据适配层，前端不依赖供应商格式。AKShare 是 Python 库，不作为 Node.js 的直接依赖；如果后续使用，应增加独立采集服务并明确额外运维成本。首版优先选择可直接通过 HTTP 调用的数据源。

### 6.1 刷新策略

- 优先使用缓存；交易时段股票与加密货币的行情缓存目标为 15 分钟，具体受数据权限约束。
- 用户点击刷新时，仅重新请求过期行情，并展示刷新进度和限频结果。
- 每个用户手动刷新至少间隔 60 秒；相同标的请求由服务端合并。
- 基金、日频汇率、黄金日行情按供应商发布节奏更新，不伪装成实时数据。
- 后台只采集活跃持仓涉及的标的，结合调用额度调整频率。
- “休市”和“接口故障”分别识别；陈旧阈值按市场日历与预期发布周期设置。
- 接口超时或限流时指数退避重试，保留最近有效报价；不反复触发全量请求。
- API 密钥只保存在服务端；日志不输出密钥或用户完整资产明细。

## 7. 响应式与交互要求

| 屏幕范围 | 布局 |
| --- | --- |
| 小于 768px | 单列卡片、底部导航；资产列表转为卡片；录入使用独立页面 |
| 768–1199px | 双列布局，紧凑导航；列表按空间选择表格或卡片 |
| 1200px 及以上 | 侧边导航、多列统计卡；图表与明细并排 |

- 最小验证宽度 360px；页面无整体横向滚动，长资产名称可合理换行。
- 移动端主要触控区域不小于 44×44px。
- 表单支持正确的邮箱、密码、数字键盘与自动填充。
- 手机数字键盘不能遮挡确认按钮；保留安全区间距。
- 图表支持触摸查看日期和数值，并提供可读摘要；关键信息不依赖悬停。
- 涨跌同时使用正负号、文字与颜色表达，不只依靠红绿区分。
- 金额可一键隐藏，隐藏设置保存在当前设备。
- 首屏加载展示骨架屏，局部失败允许单独重试，不清空已加载资产。
- 写入失败保留表单内容；重复提交不得生成重复流水。
- 删除、撤销等影响历史的操作需说明具体影响；普通查看、筛选不增加确认步骤。

## 8. 数据模型与技术边界

### 8.1 核心实体

| 实体 | 主要字段 / 职责 |
| --- | --- |
| User | 用户 ID、规范化邮箱、密码哈希、创建时间；保留认证框架的验证状态字段，不作为访问条件 |
| Session / AuthToken | 会话、重置令牌摘要、过期和撤销时间 |
| Account | 用户 ID、账户名称、类型、归档状态 |
| Instrument | 资产类型、名称、代码、市场、报价币种、单位、供应商映射 |
| Transaction | 用户 ID、事件类型、发生时间、备注、状态、幂等键 |
| TransactionLeg | 关联事件、账户、资产、数量增减及现金变动；关联腿原子提交 |
| Holding | 用户、账户、标的、当前数量；由有效流水推导或维护缓存 |
| Quote / FXRate | 数值、有效时间、采集时间、来源、质量状态 |
| ManualQuote | 用户私有的参考价格、原因、生效时间，不影响其他用户 |
| ValuationSnapshot | 用户、估值时点、总额/已估值合计、现金流、完整性、版本 |
| SnapshotItem | 快照明细、持有数量、所用价格与汇率及时间 |
| AuditLog | 敏感操作、流水修正、快照重算记录 |

### 8.2 建议架构

- 前端：React + TypeScript，分离服务端数据状态和本地交互状态。
- 后端：Node.js + TypeScript，框架在实施时选择；提供认证、资产、流水、行情和统计模块。
- 数据库：PostgreSQL，支持事务、十进制字段与多用户隔离。
- 后台任务：负责行情采集、快照、邮件和重算；与 Web 请求解耦，部署时可先共用一个服务实例。
- 前端与 API 优先同源部署，简化 Cookie、跨域和 CSRF 控制。
- 托管行情查询与资产计算均在后端执行，客户端展示结果。
- 所有私有查询和变更必须按当前登录用户校验，不能信任前端提交的用户 ID。

### 8.3 API 能力清单

- 认证：注册、登录、退出、当前用户、找回和重置密码。
- 账户：创建、查询、修改、归档。
- 标的：搜索、获取详情、查询行情与历史。
- 资产：初始化、持仓列表、持仓详情。
- 流水：新增、分页查询、修正、撤销。
- 统计：当前估值、刷新任务、资产占比、历史快照、累计盈亏。
- 数据：CSV/JSON 导出。

写入 API 使用幂等键；资金和持仓相关变动必须在数据库事务中完成。服务端统一返回字段校验错误、权限错误、行情不可用和限流状态。

## 9. 安全、隐私与运维

- 密码采用成熟的密码哈希方案，如 Argon2id；不保存明文或可逆加密密码。
- 会话使用 HttpOnly、Secure、适当 SameSite 策略的 Cookie，写请求实施 CSRF 防护。
- 登录、注册、密码找回和行情刷新均有限流。
- 全站 HTTPS；Cookie、邮件令牌、行情密钥不进入普通业务日志。
- 用户之间的持仓、流水、导出、统计、手动报价均需隔离测试。
- JSON/CSV 导出仅包含当前用户数据；CSV 防止电子表格公式注入。
- 数据库每日备份，保留最近 30 天，至少完成一次恢复演练。
- 记录行情任务失败、快照缺失、邮件失败和 API 错误率，提供运维告警。
- 不接入真实交易账户密钥，不采集与产品功能无关的身份信息。

## 10. 性能与可靠性目标

- 以单用户 200 条持仓、10,000 条流水为首版验证规模。
- 有缓存时，总览接口 P95 响应时间不超过 1 秒，图表接口不超过 2 秒，测试环境和负载需记录。
- 不把外部行情接口耗时混入首屏阻塞请求；刷新可异步完成。
- 在约定的移动网络测试条件下，主要首屏内容目标在 3 秒内可见。
- 快照任务正常情况下在设定时点后 15 分钟内完成；失败自动重试并提示异常。
- 任务重复执行不产生重复快照或重复流水，用户刷新页面不重复提交交易。

## 11. 验收标准

以下数值仅为验收样例，不代表真实市场行情。

| 编号 | 场景 | 预期结果 |
| --- | --- | --- |
| A01 | 新用户完成注册，无需验证邮箱 | 自动登录并进入空资产引导 |
| A02 | 密码重置链接过期、重复使用 | 拒绝使用，并提供重新申请入口 |
| A03 | 用户 A 请求用户 B 的持仓、流水或导出 | 返回无权限/资源不存在，不泄漏数据 |
| A04 | 持有 1,000 USD，汇率 7.20 | 人民币价值显示 7,200.00 元 |
| A05 | 持有 10 股美股，价格 200 USD，汇率 7.20 | 人民币价值显示 14,400.00 元 |
| A06 | 上日 100,000 元，转入 10,000 元，期末 112,000 元 | 资产变化 +12,000 元，投资盈亏 +2,000 元 |
| A07 | 两个已纳入统计的账户间转账，无费用 | 总资产与外部净流入不变 |
| A08 | 用已有美元买股票，价格和汇率不变 | 现金减少、股票增加，总资产仅因费用减少 |
| A09 | 某持仓首次查询失败，没有历史报价 | 不填零；显示已估值合计和未估值项数，盈亏不确定展示 |
| A10 | 股票周末休市，加密货币继续更新 | 股票沿用收盘价并标记，币价正常更新 |
| A11 | 只录入今天的持仓 | 不生成今天之前的真实持仓价值曲线 |
| A12 | 修改一条已影响历史的有效流水 | 记录修正日志，重算相关快照并显示重算状态 |
| A13 | 360px、768px、1440px 三种宽度 | 可完成登录、添加资产、记流水、看图表，无页面横向溢出 |
| A14 | 重复提交同一交易请求 | 只产生一个完整交易事件 |
| A15 | 卖出数量超过持仓或买入现金不足 | 阻止提交，并说明不足数量或金额 |
| A16 | 2:1 拆股，价格相应减半，无其他变化 | 数量翻倍，资产价值不凭空改变 |
| A17 | 外币资产原币价格不变，人民币汇率变化 | 人民币估值随汇率变化，历史使用各时点汇率 |
| A18 | 行情接口故障后恢复 | 保留有效历史报价，恢复后更新；手动报价切换有明确提示 |
| A19 | 用户重置密码后使用原会话访问 | 原会话失效，需要重新登录 |
| A20 | 导出后核对持仓、流水和快照 | 与当前用户数据一致，包含币种、时间和来源字段 |

## 12. 开发里程碑

### M0：数据源验证

- 每种资产至少选一个代表标的验证当前价、历史价、币种、单位和时间。
- 确认免费/付费额度、授权范围、故障策略与服务器网络可达性。
- 未通过自动报价验证的类型必须保留手动路径，不能宣称自动行情已支持。

### M1：账户与资产基础

- 完成响应式页面框架、注册登录、密码找回、账户管理与期初资产录入。
- 完成跨用户隔离、注册自动登录和密码找回邮件闭环。

### M2：当前估值

- 完成行情适配层、汇率换算、手动报价、总资产与资产占比。
- 完成缺价、陈旧报价、超时与限流处理。

### M3：流水与历史

- 完成交易事件、现金腿、每日快照、历史重算、资产与盈亏曲线。
- 使用固定价格与汇率样例验证完整计算链路。

### M4：上线自用

- 完成移动端验收、数据导出、备份恢复、HTTPS 与监控。
- 部署到正式环境，验证关闭个人电脑后仍能访问与执行定时任务。

## 13. 产品成功标准

- 用户能够独立完成注册，并录入至少三种资产。
- 自动报价覆盖的持仓能准确换算成人民币，且来源与时间可追溯。
- 连续运行 7 天能够查看真实积累的资产变化曲线。
- 新增存款、内部转账和买卖不会制造虚假投资盈利。
- 手机可完成所有 P0 操作，无需切换到电脑。

## 14. 参考资料

以下链接为方案调研入口，具体供应商权限与价格以接入时官方说明为准。

- [Frankfurter 汇率 API](https://frankfurter.dev/)
- [Twelve Data API 文档](https://twelvedata.com/docs)
- [Twelve Data 套餐与数据权限](https://twelvedata.com/pricing)
- [CoinGecko API](https://www.coingecko.com/en/api)
- [Tushare 公募基金净值接口](https://tushare.pro/document/2?doc_id=119)
- [AKShare 股票数据文档](https://akshare.akfamily.xyz/data/stock/stock.html)
- [AKShare 黄金现货数据文档](https://akshare.akfamily.xyz/data/spot/spot.html)
