# 验证记录

## 2026-09-19：PostgreSQL + Prisma 迁移（当前版本）

- 本机 PostgreSQL 14.7，Prisma / @prisma/client / @prisma/adapter-pg 7.10.0；Better Auth 已使用 Prisma Adapter，运行时代码不再访问 SQLite。
- 19 张原表、419 条记录事务导入成功；对所有原字段逐条比较（认证时间统一 UTC、布尔类型规范化），密码哈希、十进制字符串、快照与会话均一致。原 SQLite 文件和迁移前一致性备份保留。
- TypeScript、Prettier、Next.js Webpack 生产构建通过；26 项 PostgreSQL 自动化测试全部通过。测试使用独立 asset_atlas_test 数据库，每个测试文件独立 schema，结束自动清理。
- 新增验证：8 个并发初始化/同键记账请求只创建一份记录；事务异常回滚；pg_dump 备份恢复到临时数据库后核对流水、快照，备份权限 0600。
- 原浏览器登录无需重新认证，13 项原有持仓正常显示；开发服务继续使用 localhost:3003。
- 生产 standalone 在隔离 PG schema 中验证启动、静态资源、API 认证和私有页面重定向。
- Docker Compose 使用独立测试项目、PG 15 持久卷和虚构账户：db 健康，Prisma migrate 任务退出码 0，app 健康；注册自动登录、十进制持仓保存与估值通过。非 root 运行镜像中的 pg_dump / pg_restore、备份卷写入通过。测试容器与测试数据卷已清理。
- 报价改为批量 Prisma 关联读取。200 项持仓、10,000 条事件、30 次估值测试（本机同时构建容器）观测 p50 294ms、p95 664ms；这只是服务层 + 本地 PG 测量，不代表完整页面或移动网络性能。
- 本机旧 PostgreSQL 14 命令行客户端无法正常启动；本地备份已配置 scripts/pg-dump 与 scripts/pg-restore，通过官方 PG 15 Docker 临时客户端执行，真实 PG 备份已生成。Docker 生产镜像内置原生客户端，不依赖嵌套 Docker。

下方保留早期 SQLite 版本的验收记录，历史性能和迁移实现不代表当前版本。

日期：2026-09-18；环境：macOS / Node.js 22.20.0 / 本机 Chrome / SQLite。

## 已执行

- TypeScript 严格类型检查通过。
- Next.js 生产构建通过（Webpack）；开发服务单进程运行于 3003。
- 实际启动生产 standalone 临时实例，验证总览演示、JS 静态资源、未登录私有 API 401、登录原路返回；完成后已关闭临时实例。
- 18 项自动化测试通过：16 项账本/金额/历史规则、1 项认证 API 集成、1 项备份恢复。
- 认证集成：注册自动登录、未验证账户允许登录、不发送注册邮件、登录 Cookie、未登录 401、他人资源 404、Origin 拒绝、导出隔离、CSV 公式注入、密码重置及旧会话失效、重置令牌重复使用拒绝。
- 浏览器：360 / 768 / 1440px，演示总览、资产、流水、设置、详情、登录、注册共 21 个布局检查通过。
- 已登录总览、资产录入、流水录入、设置在三种宽度共 12 个布局检查通过。
- 首版浏览器真实表单完成“注册 → 邮箱验证（已按新需求取消） → 期初现金 100,000 → 建立基线 → 外部入金 10,000”；接口核对总资产 110,000、净流入 10,000、盈亏 0。
- 金额隐藏状态刷新后保留；浏览器运行错误为 0。
- 在线备份恢复成新 SQLite 文件，`integrity_check=ok`，流水及快照记录数一致。
- Frankfurter USD→CNY/HKD 与 CoinGecko Bitcoin/USD 当前接口实际请求返回 HTTP 200；没有外部行情密钥参与测试。

## 性能样本

内存 SQLite，200 项持仓、10,000 个事件，估值服务调用 30 次：

| 指标   | 耗时     |
| ------ | -------- |
| P50    | 33.34 ms |
| P95    | 42.64 ms |
| 最大值 | 47.89 ms |

这是后端估值函数的本地样本，**不包含 HTTP、外部行情或移动网络耗时**，不能代替生产总览接口 P95 或首屏 3 秒验收。

## PRD 验收对照

| 编号    | 结果与边界                                                                      |
| ------- | ------------------------------------------------------------------------------- |
| A01     | 注册自动登录、未验证账户直接登录已验证，注册不依赖邮件                          |
| A02     | 密码重置链接重复使用拒绝；旧验证页面兼容跳回登录；有效期由 Better Auth 配置控制 |
| A03     | 账户、持仓、流水、导出和私有报价隔离已测                                        |
| A04–A09 | 对应十进制估值、现金流、原子买卖、缺价行为已测                                  |
| A10     | 保留历史报价；完整节假日/停牌识别未完成，不标称准确休市日历                     |
| A11     | 仅从基线开始记录，单点状态已实现                                                |
| A12     | 修正/撤销审计与同步原子重算已测；保存时显示处理中                               |
| A13     | 三宽度表单与页面无横向溢出，浏览器流程通过                                      |
| A14–A17 | 幂等、负余额、拆股、历史汇率已测                                                |
| A18     | 回退与手动价保留路径已实现；供应商故障恢复的线上连续测试待完成                  |
| A19     | 重置密码后旧会话 401 已测                                                       |
| A20     | JSON 隔离及持仓核对已测；CSV 三类导出与字段来源已实现                           |

## 尚需生产环境验证

1. 实际发信域名、真实邮箱收件与垃圾箱表现。
2. 股票、基金供应商密钥、授权、额度；代表性 A/H/美股及基金的自动报价覆盖。
3. 法定节假日、停牌和发布周期感知的行情状态；供应商历史价格补采。
4. HTTPS、反向代理可信 IP、持久化卷、外部错误告警、异地备份。
5. 连续 7 天快照积累、长时间停机后的真实行情完整性、真实移动网络首屏。
6. Docker 镜像实际构建启动；当前已验证本机 Next.js 生产编译，未声称 Docker 上线完成。

## 2026-09-19：图标、共同资产、MCP、部署

- TypeScript 检查及生产构建通过；26 项自动化测试全部通过。
- 图标：CoinGecko 搜索图标安全域名白名单、导入持久化、旧资产按 providerId 回填；浏览器中 RAY、USDT 等实际图片成功加载，黄金静态文件返回 200。
- 共享：邀请前禁止读取；只有受邀者能接受；本人账户校验、拒绝、过期、撤回、双向解除；三用户直接授权隔离，不传递好友关系；多人汇总按账户去重，不泄露未共享账户和流水备注。共享不会放开原持仓修改 API。
- MCP：无/错误令牌 401、非法 Origin 403、跨用户持仓拒绝；6 个基础只读方法、额外授权流水；令牌仅哈希入库，明文不进幂等响应缓存；到期/撤销生效。官方 MCP SDK Client 通过 stdio 下载脚本完成初始化、列工具和资产查询。
- 浏览器：360px 下邀请表单、MCP 配置和已接受的共同资产视图无横向溢出；生产容器虚构账户 Alice/Bob 的 100 + 200 正确显示 300；没有给真实用户创建共享关系或 MCP 密钥。
- Docker：本机 ARM64 实际构建 node:22-bookworm → node:22-bookworm-slim 两阶段镜像；运行时非 root UID 1000；Hono、Better Auth、SQLite、MCP、图标/连接脚本及在线备份均实测。独立测试卷中的数据与会话在重启后保留，健康检查 healthy。镜像无 .env.local/.env.docker 或开发密钥。
- 迁移：8 个独立 Node 进程并发初始化旧数据库通过；schema 检查和 ALTER 使用同一 IMMEDIATE 事务，解决构建阶段偶发重复列错误。定时备份测试输出隔离在临时目录，现有账本另生成有效在线备份。
- 基础 Compose 与 HTTPS override 的配置校验通过。公网域名和 ACME 证书申请需要真实部署环境，未对用户的域名或服务器执行发布。

命令：`pnpm typecheck`、`pnpm test`、`pnpm build`、`docker build -t asset-atlas:local .`。Docker 集成检查使用隔离测试卷和虚构用户，不修改开发环境账本。最终部署参见 DEPLOYMENT.md，MCP 连接参见 MCP.md。

## 2026-09-21：Vercel + Neon 免费部署适配

- 类型检查、生产构建和改动文件格式检查通过；构建包含独立 `/api/cron/daily` 路由。
- 完整测试首轮 31/32 通过，唯一失败为 Docker 未运行导致的备份恢复测试。启动已有 Docker 后重跑 `tests/migrations.test.ts`，两项均通过。
- 新增 6 项 serverless 回归测试，覆盖 Vercel 禁用常驻任务、Cron 鉴权及 Preview/HEAD 限制、并发快照与未来报价隔离、部分进度恢复、自动/手动行情冷却、MCP 并发限流、跨认证实例的登录限流。
- `node scripts/production-check.mjs` 在隔离测试 schema 上验证生产服务和 Cron，并使用 Chrome 验证自动刷新、冷却跳过、演示页隔离和设置页提示；测试账户与 schema 自动清理。
- 尚未连接真实 Neon 或发布到 Vercel；区域延迟、免费额度消耗、平台 Cron 和供应商密钥需上线后核验。上线前执行新增共享限流迁移，步骤见 [免费部署指南](VERCEL.md)。
